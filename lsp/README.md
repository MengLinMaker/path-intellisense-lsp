# @menglinmaker/path-intellisense-lsp
This module compiles into a language server.

### Development
Features should be implemented in `./src/handlers`.

### Improvements
- Add cli linting capability with config.
