{
    "name": "@menglinmaker/zed",
    "private": true,
    "scripts": {
        "boot": "cargo fetch",
        "fmt": "cargo fmt",
        "lint": "cargo clippy --fix --allow-dirty",
        "build": "cargo build --target wasm32-wasip2 --release && rm -rf ./out && mkdir -p ./out && cp ./target/wasm32-wasip2/release/path_intellisense_lsp.wasm ./out/extension.wasm && cp ./extension.toml ./out/extension.toml"
    }
}